# Dependencies for scripts/validate_live.py (manual live validation).
# Not installed by CI — the live script is deliberately excluded from it.
#
#   pip install -r scripts/requirements.txt
#   playwright install chromium
-r ../backend/requirements.txt
pyyaml>=6.0
