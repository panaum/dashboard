playwright>=1.45
# The one dependency beyond Playwright and the standard library. Used only to
# downscale thumbnails; if removed, captures still succeed and each result
# carries a note that the gallery will scale the fold image instead.
pillow>=10
\n# Pillow is required for --baseline diffing (and thumbnails). odiff is optional and\n# faster: any `odiff` on PATH is used, or pass --odiff "npx -y odiff-bin".\n
# The HTTP service (server.py) that the Dashboard talks to — the same stack as
# services/linkspy-api. Not needed to run the CLI by hand.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
