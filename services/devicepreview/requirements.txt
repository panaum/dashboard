playwright>=1.45

# Pillow: thumbnails, the JPEG variants the service hands the Dashboard, and
# --baseline diffing. Without it captures still succeed and each result carries
# a note that the gallery will scale the fold image instead.
pillow>=10

# odiff is optional, not installed here: any `odiff` on PATH is used for
# --baseline diffing, or pass --odiff "npx -y odiff-bin". Pillow does the work
# otherwise.

# The HTTP service (server.py) the Dashboard talks to — the same stack as
# services/linkspy-api. Not needed to run the CLI by hand.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
